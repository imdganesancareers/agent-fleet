# Operator flow docs

Type: task
Status: open
Blocked by: 04

## Question

Write the repo README: the full operator flow — Discord portal prerequisites (from
[Discord plugin surface](01-discord-plugin-surface.md)), authoring `agent.yaml` with
`/grill-with-docs` ("define a new agent"), running `sudo ./create-agent.sh agent.yaml`,
first-attach OAuth, rerun/reconcile semantics, and where everything lands on disk.
Also decide the fog item: whether to `git init` this repo now.
